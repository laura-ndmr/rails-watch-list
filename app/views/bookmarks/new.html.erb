<h3>Add a movie to <%= @list.name %> list</h3>

  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.submit class: 'btn' %>
  <% end %>

  <%= link_to 'Back', lists_path %>
