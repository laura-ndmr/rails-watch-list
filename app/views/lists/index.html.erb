<div class=navbar>
   <%= link_to "My list", lists_path, class: 'btn' %>

  <%= link_to "New", new_list_path, class: 'btn' %>
</div>

<div class=banner>
  <div class="container">




  </div>
</div>
  <% @lists.each do |list| %>
    <%= link_to list.name, list_path(list) %>
  <% end %>
